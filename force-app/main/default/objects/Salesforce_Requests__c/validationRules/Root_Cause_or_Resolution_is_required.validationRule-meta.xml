<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Root_Cause_or_Resolution_is_required</fullName>
    <active>true</active>
    <errorConditionFormula>AND( OR(TEXT( Status__c )= &quot;Fixed&quot;,TEXT( Status__c )= &quot;Fixed on Dev Env.&quot;),  ISBLANK(Root_Cause__c), ISBLANK(Resolution__c), ISBLANK(Fixed_by__c)   )</errorConditionFormula>
    <errorMessage>Root Cause, Resolution, and Fixed By are required when the status is &quot;Fixed&quot; or &quot;Fixed on Dev Env.&quot;</errorMessage>
</ValidationRule>
