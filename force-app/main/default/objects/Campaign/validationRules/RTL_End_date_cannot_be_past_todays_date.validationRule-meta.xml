<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RTL_End_date_cannot_be_past_todays_date</fullName>
    <active>true</active>
    <description>Preventing the end date cannot be past today’s date</description>
    <errorConditionFormula>AND(RecordType.Name != &apos;Commercial&apos; , RecordType.Name != &quot;Dummy Campaign&quot;, 
     OR(Isnew(), ISCHANGED(EndDate)) ,  
     EndDate &lt;  TODAY() )</errorConditionFormula>
    <errorDisplayField>EndDate</errorDisplayField>
    <errorMessage>The end date cannot be past today’s date after executed.</errorMessage>
</ValidationRule>
