//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class RTL_BancPrdDetailsServiceMockImpl implements WebServiceMock {
	public void doInvoke(
	                     Object stub,
	                     Object request,
	                     Map<String, Object> response,
	                     String endpoint,
	                     String soapAction,
	                     String requestName,
	                     String responseNS,
	                     String responseName,
	                     String responseType) {

		System.debug(LoggingLevel.INFO, 'RTL_BancPrdDetailsService1MockImpl.doInvoke() - ' +
		             '\n request: ' + request +
		             '\n response: ' + response +
		             '\n endpoint: ' + endpoint +
		             '\n soapAction: ' + soapAction +
		             '\n requestName: ' + requestName +
		             '\n responseNS: ' + responseNS +
		             '\n responseName: ' + responseName +
		             '\n responseType: ' + responseType);

		if (request instanceOf RTL_BancPrdDetailsService.getBancassuranceProductDetails_element) {
			RTL_BancPrdDetailsService.getBancassuranceProductDetailsResponse_element res = new RTL_BancPrdDetailsService.getBancassuranceProductDetailsResponse_element();
			RTL_BancPrdDetailsService.BancassuranceProductDetails Result = new RTL_BancPrdDetailsService.BancassuranceProductDetails();
			res.Result = Result;	
			response.put('response_x', res);
		}
	}
}