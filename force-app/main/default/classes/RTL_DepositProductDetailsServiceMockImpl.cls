//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class RTL_DepositProductDetailsServiceMockImpl implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
       
		System.debug(LoggingLevel.INFO, 'RTL_DepositProductDetailsServiceMockImpl.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

		if(request instanceOf RTL_DepositProductDetailsService.getProductDetails_element) {
		    //RTL_DepositProductDetailsService.DepositProductDetails
		    RTL_DepositProductDetailsService.getProductDetailsResponse_element res = new RTL_DepositProductDetailsService.getProductDetailsResponse_element();
			RTL_DepositProductDetailsService.DepositProductDetails detail = new RTL_DepositProductDetailsService.DepositProductDetails();
			res.Result = detail;
			response.put( 'response_x', res);
		}
	}
}