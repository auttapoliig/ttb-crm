@IsTest
public class TestCSVStream {
/*     static testmethod void TestUtilsTest(){        
        Test.startTest();
         String userReport;
         List<Report> rpID = [Select id, name from Report limit 1]; 
         String reportID = rpID[0].Id;
         CSVStream csvreport = new CSVStream();      
            csvreport.strRptname = reportID;
            csvreport.executeRpt(); 
            userReport = csvreport.getCSVStream();
         Boolean isSend = false;
         if(userReport != null){
             isSend = true;
         }
         system.assertEquals(true, isSend);
        Test.stopTest();
     }
*/
}