//Generated by FuseIT WSDL2Apex (http://www.fuseit.com/Solutions/SFDC-Explorer/Help-WSDL-Parser.aspx)

@isTest
public class wsapiEcmClevelComMockImpl implements WebServiceMock {
	public void doInvoke(
			Object stub,
			Object request,
			Map<String, Object> response,
			String endpoint,
			String soapAction,
			String requestName,
			String responseNS,
			String responseName,
			String responseType) {
       
		System.debug(LoggingLevel.INFO, 'wsapiEcmClevelComMockImpl.doInvoke() - ' +
			'\n request: ' + request +
			'\n response: ' + response +
			'\n endpoint: ' + endpoint +
			'\n soapAction: ' + soapAction +
			'\n requestName: ' + requestName +
			'\n responseNS: ' + responseNS +
			'\n responseName: ' + responseName +
			'\n responseType: ' + responseType);

		if(request instanceOf wsapiEcmClevelCom.createDocument) {
			response.put( 'response_x', new wsapiEcmClevelCom.createDocumentResponse());
		}
		else if(request instanceOf wsapiEcmClevelCom.getContentStream) {
			response.put( 'response_x', new wsapiEcmClevelCom.getContentStreamResponse());
		}
	}
}